# Utkarsh Organic Admin

React admin panel for the Utkarsh Organic backend.

```bash
npm run admin:dev
```

The app expects the backend at `http://localhost:5000/api`. Override with:

```env
VITE_API_URL=http://localhost:5000/api
```
